import { Poppins } from 'next/font/google';

export const typeFirst = Poppins({
  weight: ['200', '300', '400', '500', '600', '700'],
  subsets: ['latin'],
  display: 'auto',
  variable: '--type-first-poppins'
});
